# FileHandlingAndFileOrganizer
Python File &amp; Folder Management Utility  This tool provides a log-enabled, menu-driven system for file and folder operations. It features a high-level FileOrganizer class that uses a JSON map for automated file sorting into categories (like Documents, Images), handling conflicts robustly.
